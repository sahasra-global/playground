<h1>Set 09 | Lakshmi | JavaScript</h1>
<h3>Problems solved in the files: </h3>
<ul>
    <li>Find the maximum number in a jagged array of numbers or array of numbers</li>
    <li>Deep copy a jagged array with numbers or other arrays in a new array</li>
    <li>Create a function to return the longest word in a string</li>
    <li>Shuffle an array of strings</li>
    <li>Create a function that will receive n as argument and return an array of n random numbers from 1 to n. The numbers should be unique inside the array.</li>
    <li>Find the frequency of letters inside a string. Return the result as an array of arrays. Each subarray has 2 elements: letter and number of occurrences.</li>
</ul>